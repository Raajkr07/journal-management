spring:
  data:
    mongodb:
      uri: YOUR_MONGODB_ATLAS_URI
      name: journal App
      database: journalDB
      auto-index-creation: true

server:
  port: 8081
  servlet:
    context-path: /journal